
@*@using ApplicationLayer.Authentication
@using System.Security.Claims
@inject AuthenticationStateProvider aus
@inject NavigationManager nm
@inject IJSRuntime js

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">ApplicationLayer</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <CascadingAuthenticationState>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                    <span class="oi oi-home" aria-hidden="true"></span> Home
                </NavLink>
            </div>

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="Categories">
                    <span class="oi oi-list" aria-hidden="true"></span> Categories
                </NavLink>
            </div>

            <AuthorizeView Roles="seller">
				<Authorized>
					<div class="nav-item px-3">
						<NavLink class="nav-link" href="BookListing">
							<span class="oi oi-book" aria-hidden="true"></span> Book Details
						</NavLink>
					</div>

                    

                    <NavLink class="nav-link">
                        <button @onclick="Logout">Logout</button>
                    </NavLink>
                </Authorized>

                <NotAuthorized>

                    <div class="nav-item px-3">
                        <NavLink class="nav-link" href="Register">
                            <span class="oi oi-account-login" aria-hidden="true"></span> Register
                        </NavLink>
                    </div>               
                  
                </NotAuthorized>
            </AuthorizeView>

            <AuthorizeView Roles="buyer">
                <Authorized>
                  
                    <div class="nav-item px-3">
                        <NavLink class="nav-link" href="BookListing">
                            <span class="oi oi-book" aria-hidden="true"></span> Book Details
                        </NavLink>
                    </div>

                    <NavLink class="nav-link">
                        <button @onclick="Logout">Logout</button>
                    </NavLink>
                </Authorized>
            </AuthorizeView>

            <AuthorizeView Roles="Admin">
                <Authorized>

                    <div class="nav-item px-3">
                        <NavLink class="nav-link" href="adminpage">
                            <span class="oi oi-book" aria-hidden="true"></span> Dashboard
                        </NavLink>
                    </div>
                  
                        <NavLink class="nav-link">
                            <button @onclick="Logout">Logout</button>
                        </NavLink>
                </Authorized>
             </AuthorizeView>
        </CascadingAuthenticationState>
    </nav>
</div>


@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;
    private async Task Logout()
    {
        var cas = (CustomAuthenticationStateProvider)aus;
        await cas.UpdateAuthenticationStae(null);
        nm.NavigateTo("login");
    }
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
   
    private async Task SelectSeller()
    {
        // Get the authenticated user's information
        var authState = await aus.GetAuthenticationStateAsync();
        var user = authState.User;

        // Use the user's ID as the selected seller's ID
        var selectedSellerId = user.FindFirstValue(ClaimTypes.NameIdentifier);

        // Now, navigate to the Inbox page with the selected seller's ID
        nm.NavigateTo($"/seller-chat/{selectedSellerId}");
    }
}*@

@using ApplicationLayer.Authentication
@using System.Security.Claims
@inject AuthenticationStateProvider aus
@inject NavigationManager nm
@inject IJSRuntime js


<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <div class="flex-row">
        <NavLink class="nav-item px-3 nav-link" href="" Match="NavLinkMatch.All">
            <span class="oi oi-home" aria-hidden="true"></span> Home
        </NavLink>

        <NavLink class="nav-item px-3 nav-link" href="Categories">
            <span class="oi oi-list" aria-hidden="true"></span> Categories
        </NavLink>

        <AuthorizeView>
            <Authorized>
                <NavLink class="nav-item px-3 nav-link" href="BookListing">
                    <span class="oi oi-book" aria-hidden="true"></span> Book Details
                </NavLink>

                <button class="nav-link" @onclick="Logout">Logout</button>
            </Authorized>
        </AuthorizeView>
    </div>
</div>
@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private async Task Logout()
    {
        var cas = (CustomAuthenticationStateProvider)aus;
        await cas.UpdateAuthenticationStae(null);
        nm.NavigateTo("login");
    }

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}

